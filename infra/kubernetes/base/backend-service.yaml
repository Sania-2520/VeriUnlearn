apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: veriunlearn
  labels:
    app.kubernetes.io/name: veriunlearn
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: veriunlearn
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: veriunlearn
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
